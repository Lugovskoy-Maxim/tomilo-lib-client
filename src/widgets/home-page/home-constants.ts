/**
 * Единый вертикальный шаг между блоками главной (`<main>` flex gap).
 * Раньше было `gap-3 sm:gap-4 md:gap-6` — на разных брейкпоинтах визуально «плывало».
 */
export const HOME_MAIN_GAP_CLASS = "gap-6 sm:gap-7 md:gap-8";
